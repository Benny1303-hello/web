node_modules
.next
out
.vercel
.env*.local
!.env.local.example
npm-debug.log*
.DS_Store
.claude/settings.local.json
.env*

# Raw extracted supplier price data (wholesale/dealer pricing — must never
# reach the public repo). Produced by the product-excel-extract skill.
data/product-imports/
